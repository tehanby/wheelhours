from setuptools import setup, find_packages

setup(
    name='WheelHours',
    version='0.1',
    packages=find_packages(),
    description='A project for managing wheel hours.',
    author='Your Name',
    author_email='your.email@example.com',
)
